name: Generar APK

echo "GRADLE=$GRADLE"
find proyecto -maxdepth 4 -type f | head -n 100
  workflow_dispatch:
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Descargar proyecto
        uses: actions/checkout@v4

      - name: Preparar Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Descomprimir Android.zip
        run: |
         unzip -o AlertaYPreparados-Android.zip -d proyecto 
      - name: Buscar Gradle
        run: |
         GRADLE=$(find proyecto -name gradlew -type f | head -n 1)
echo "GRADLE=$GRADLE"
find proyecto -maxdepth 4 -type f | head -n 100
          chmod +x "$GRADLE"
          echo "GRADLE=$GRADLE" >> $GITHUB_ENV

      - name: Compilar APK
        run: |
          cd "$(dirname "$GRADLE")"
          ./gradlew assembleDebug

      - name: Guardar APK
        uses: actions/upload-artifact@v4
        with:
          name: APK-AlertaYPreparados
          path: "**/build/outputs/apk/**/*.apk"
